#include <gtest/gtest.h>

#include "src/StringUtils.h"

TEST(StringUtilsTest, throws_if_offset_is_too_large) {
  ASSERT_FALSE(true);
}

TEST(StringUtilsTest, handles_offset_larger_than_one_buf_in_the_chain) {
  ASSERT_FALSE(true);
}

TEST(StringUtilsTest, defaults_to_zero_offset) {
  ASSERT_FALSE(true);
}

TEST(StringUtilsTest, handles_single_buffer_with_offset) {
  ASSERT_FALSE(true);
}
